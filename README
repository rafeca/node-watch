node-watch
===========
Simple utility to watch file changes

This utility is handy for e.g. automatic testing in combination with a testing frame-work,
An example lives within the JakeFile

 run: jake autotest

and make changes in the lib/watch/watch.js file or the spec/watchSpec.js file

usage:
-----
 var watch = require('./deps/watch/watch.js');
 // Adding 2 dirs relative from process.cwd()
 // Nested dirs are not watched
 // dirs can also be added absolute
 watch.addDir("./spec").addDir("./lib/watch").onChange(function(file,prev,curr){
		console.log(file);
		console.log(prev.mtime);
		console.log(curr,mtime);
 });

 // Clear (remove) the listeners
 watch.clearListeners();

 // Remove dirs to watch
 watch.remdDir("./spec").remdDir("./lib/watch");

